# Retarget чужої зброї на наші руки — двоходовий своп

Валідований метод перенесення **чужої (foreign) зброї** на **наші руки** (CSO Hand
2009) зі збереженням рідної ідентичності зброї та її анімацій. Перший наскрізний
прохід зроблено на **Valve Deagle** (`tmp/valve_original/v_deagle`), еталон хвату —
**Nexon AutomagV** (`tmp/cso_nexon/pistols/v_deagle_automagv`).

> TL;DR: не «ретаргетити» чужу зброю на нашу Bip01-руку (це провалюється — розкриті
> пальці, топлення великого, розмірний конфлікт). Натомість **позичити правильний
> хват у native-CSO-зброї нашого розміру** двома свопами.

---

## 1. Проблема і чому пряме перенесення не працює

Ціль: чужа зброя (напр. Valve deagle) має **маленькі руки** оригіналу. Наші руки —
CSO 2009, помітно більші (пальці ~16% довші; діагональ долоні інша). Пряме
перенесення (архетипна поза → наша Bip01-рука) стабільно дає:

- **розкриті пальці** — недокручений PIP (наш архетип PIP 35–70°, а треба 83–95°);
- **топлення великого пальця** — це база/опозиція (анатомія), не згин; згином не лікується;
- **бугор долоні** — наша долоня опукліша за пласку оригінальну; вершина тоне (сховано);
- зсув зброї (Location) лише **перекладає** кліпінг з однієї частини на іншу, не прибирає.

Висновок з експериментів: пряма трансляція/поза не розв'язують розмірний конфлікт.
Норма для CSO viewmodel: пальці й так тонуть у ствол на **~0.1u** навіть у авторському
native-хваті — тонення схованого/внутрішнього боку є нормальним, з ним не треба воювати.

---

## 2. Метод: двоходовий своп

### Передумова: підбір native-оракула за розміром
Знайти **native-CSO-зброю нашого розміру** (наша рука тримає її коректно з коробки).
Для deagle такою є **AutomagV** — той самий розмір:

| ствол | габарити (довж/вис/товщ) | діагональ | трикутники |
|---|---|---|---|
| Valve Deagle | 8.78 / 4.95 / 1.05 | 10.14 | 457 |
| Nexon AutomagV | 8.90 / 4.43 / 1.20 | 10.02 | 6600 |
| відношення | 1.01 / 0.89 / 1.14 | **0.99** | — |

AutomagV на нашій руці (`hand_High_512.smd` ≈ 1130 верт CSO), деагл — на малій
(`lhand.smd` ≈ 153 верт). Обидві моделі — **той самий риг** (див. §4).

### Своп 1 — «оракул хвату» (`tmp/testing/build_swap.py`)
Взяти **весь AutomagV-сетап** (рука + скелет + native-поза + анімації) і **підмінити
лише меш зброї**: перенести деаглячий ствол покістково на ті самі ствольні кістки.

- Для кожної деаглячої weapon-вершини (кістка `B`): `v_local = deagle_bind[B]⁻¹ · v`,
  потім `v_world = automag_idle[B] · v_local` — тобто розмістити в automag-кістці `B`.
- Скін на automag-ствольні кістки; bind = automag idle[0]; аніми automag.

Результат: деагл, який **наша рука тримає з правильним тугим хватом** — але з
**анімаціями AutomagV**. Це дає **правильну позу хвату як оракул**.
Артефакт: `tmp/testing/build_swap/v_deagle_swap.mdl`.

### Своп 2 / «Варіант B» — фінал (`tmp/testing/build_variantB.py`)
Взяти **деагл-базу** (рідний скелет + ствол + усі рідні анімації) і **підмінити лише
меш руки** на нашу CSO-руку, посаджену в правильний хват із Свопу 1.

Ключовий трюк (без переписування скелета):
- Запекти вершини нашої руки у **позиції правильного хвату** (світові координати),
  скіновані на деаглячі кістки; **bind = deagle idle[0]**.
- На idle рантайм застосовує `frame_bone · idle_bone⁻¹ = identity` → показує правильний хват.
- На інших кадрах застосовує **рух деагля відносно idle** → пальці рухаються за
  **рідною** анімацією деагля, стартуючи з правильного хвату.
- Ствол — рідний деаглячий (на рідних кістках), тож слайд/магазин/reload — власні.

Результат: **рідна ідентичність зброї + рідні анімації + правильний хват наших рук**.
Артефакт: `tmp/testing/build_vb/v_deagle_vb.mdl` (обидві руки, текстури, атачменти).

---

## 3. Технічні деталі (щоб відтворити)

- **idle[0] брати з АНІМАЦІЇ, не з reference.** У reference-SMD рука й зброя часто НЕ
  співмісні (різні простори); в анімації позиції завжди коректні. FK кадру 0 анімації
  дає правильний спільний простір.
- **Міст кадрів рук (hand-frame bridge)** для перенесення нашої руки на чужу базу:
  ```
  aa = _hand_align(automag_idle_pos, palm='Bone_Lefthand', bases=['Bone21','Bone17','Bone13','Bone09'])
  ad = _hand_align(deagle_idle_pos,  palm='Bone_Lefthand', bases=['Bone21','Bone17','Bone13','Bone09'])
  bridge = ad.inverse().compose(aa)     # our-hand world -> target-hand world
  ```
  (bases = index/middle/ring/pinky базові кістки; для правої руки —
  palm='Bone_Righthand', bases=['Bone31','Bone35','Bone39','Bone43'].)
- **World-delta bake**: `posed_vert = delta[bone].transform_point(v.position)`, де
  `delta = W_frame[bone] · W_bind[bone]⁻¹`. Нормалі — `delta.rotate_vector(v.normal)`.
- **Мапінг кісток руки**: пальці — ті самі імена в обох ригах (`Bone05/06/07` thumb,
  `Bone09-11` pinky, `Bone13-15` ring, `Bone17-19` middle, `Bone21-23` index; права рука
  `Bone27-45`). Долоня/передпліччя AutomagV (`Bone_Se_Hand-L`) → деаглячий `Bone_Lefthand`.
- **Ствольні кістки** (обидва риги): `Bone_Lefthand → Bone02 → Bone03 → [Bone52 слайд,
  Bone48, Bone25 курок, Bone50, Bone54, Bone_Bullet]`. Атачменти: дуло `Bone 04`, гільза `Bone52`.
- **Компіляція**: `tmp/studiomdl` (native macOS arm64, стокові ліміти). QC поряд зі
  SMD/BMP, forward-slash шляхи. `$cliptotextures` зашиває текстури в .mdl.
- **Vertexless-bone prune**: якщо включити лише одну руку, protilежні кістки без
  геометрії пруняться (43→25). З обома руками — усі 43 лишаються.

---

## 4. Порівняльний аналіз AutomagV ↔ Deagle (для підбору)

| аспект | AutomagV (еталон) | Valve Deagle |
|---|---|---|
| Риг зброї | `Bone_Lefthand→Bone02→Bone03→[52/48/25/50/54]` | та сама структура |
| Кістки пальців | Bone05/06/07 … Bone21/22/23 | ті самі імена (1:1) |
| Розмір ствола | діаг. 10.02 | діаг. 10.14 (×0.99) |
| Зброя↔рука offset (Bone02 від Bone_Lefthand) | (-0.56, -4.52, 0.84) | (-0.37, -3.56, 0.71) |
| Рука | наша CSO (~1130 верт) | мала (~153 верт) |
| Хват (interior bend MCP/PIP) | тугий: middle 37/93, ring 40/95, pinky 28/83 | розкритий: 58/70, 64/63, 63/35 |

Головне для підбору: **однаковий риг + однаковий розмір ствола**. Тоді хват і положення
переносяться майже 1:1.

---

## 5. Межі методу (чесно)

- **Потрібен native-збіг за розміром.** Метод спирається на CSO-зброю нашого розміру як
  оракул. 141 native покриває багато розмірів, але для зброї без збігу — інший підхід
  (масштаб / інтерполяція між кількома native).
- **Хват «позичений»** під форму native-ствола; при різних пропорціях (deagle трохи
  вищий/тонший за AutomagV) — близько, але не піксель-у-піксель; можлива дрібна per-weapon
  поліровка.
- **Рух пальців в аніміях — чужий (цільової зброї)**, стартує з нашого (більшого) хвату.
  idle/shoot — чудово; агресивний reload варто звіряти окремо.

---

## 6. Наступні кроки

1. **Масштабувати** на решту ~52 чужих зброй: для кожної підібрати native того ж розміру
   → прогнати конвеєр.
2. **Автоматизувати**: з `build_swap.py` + `build_variantB.py` зробити один інструмент
   «чужа зброя → знайди native-збіг за розміром → видай .mdl».
3. **Каталог native-оракулів** за розмірами (141 CSO-зброя нашого розміру).
4. **Полірування** окремих зброй (права рука в reload, дрібні per-weapon правки).

---

## Артефакти (перший прохід — deagle)

- `tmp/testing/build_swap.py` — Своп 1 (оракул хвату).
- `tmp/testing/build_variantB.py` — Своп 2 / Варіант B (фінал).
- `tmp/testing/build_vb/v_deagle_vb.mdl` — **фінальна модель** (обидві руки, рідні
  текстури, атачменти, 6 анімацій). Перевірено в HLAM — усе штатно.

---

## 7. Відкриття: хват — кусково-константний (каталог архетипів)

Обмір хвату (кути згину MCP/PIP кожного пальця в idle[0]) по **79 nexon-зброях з нашим
ригом** показав головне: **хват НЕ обчислюється per-weapon — автори CSO працювали з
маленькою бібліотекою поз руки й копіювали їх на різні стволи**.

Докази (робастно, на рівні файлу):
- 34 зброї з повною лівою рукою → лише **16 різних поз**; найбільший кластер — **8
  стволів з ІДЕНТИЧНОЮ позою**: deagle, deaglegs, deaglered, glockred, bglock18, **ak47,
  ak47dragon, g3sg1**. Пістолет, глок, автомат, снайперка — одна поза лівої руки.
- Ротації кісток збігаються **до 4-го знака** (0.0000 різниці) — це copy-paste, не «схоже».
  Відрізняється лише вказівний (курок): в AK трохи пряміший (Bone21 -0.2356 vs -0.349).
- 79 зброй з правою рукою → 52 пози; найбільший спільний кластер — **13 стволів**.

Наслідок: **немає 141 хвата — є ~8–16 архетипів**. «Golden grip» — не один випадок
AutomagV, а невеликий **перелічуваний каталог**, що вже лежить у nexon-idle і вже на
нашому ригу. Для решти valve-зброї per-weapon оракул не потрібен: класифікуєш руків'я →
береш архетип → мерджиш.

### Каталог архетипів (grip-рука, PIP середній/безім/мізинець, град.)
| ID | назва | PIP m/r/p | базові члени (nexon) |
|----|-------|-----------|----------------------|
| A1 | pistol-grip | 70/63/35 | deagle, glock, ak47, g3sg1, aug, tmp, xm1014 |
| A2 | USP | 69/63/53 | usp (+ sg552, m3 — тугіші) |
| A3 | MP5 | 55/42/31 | mp5, bmp5 |
| A4 | AWP | 51/47/35 | awp |
| A5 | Scout | 39/35/28 | scout |
| A6 | P90 | 59/53/70 | p90 |
| A7 | grenade-rest | 45/61/40 | flashbang, he, smoke |
| R1 | one-hand pistol | 88/90/20 | elite, fiveseven, ump45, sg550 |
| K1 | knife full-fist | ~85/94/84 | ножі |

### Таблиця valve(28) → архетип
Надійність: **exact** = точний nexon-двійник (стандартний риг); **sib** = сиблінг;
**infer** = висновок за класом (двійник ненадійний/відсутній).

| valve | клас | архетип | джерело | надійн. |
|-------|------|---------|---------|---------|
| v_ak47 | rifle | A1 | v_ak47 | exact |
| v_aug | rifle | A1 | v_aug | exact |
| v_awp | sniper | A4 | v_awp | exact |
| v_c4 | gear | — | — | special (не хват) |
| v_deagle | pistol | A1 | v_deagle | exact ✅ зроблено |
| v_elite | pistol×2 | R1 | v_elite | exact |
| v_famas | rifle | A1 | aug-аналог | infer |
| v_fiveseven | pistol | R1 | v_fiveseven | exact |
| v_flashbang | gren | A7 | v_flashbang | exact |
| v_g3sg1 | sniper | A1 | v_g3sg1 | exact |
| v_galil | rifle | A1 | ak-аналог | infer |
| v_glock18 | pistol | A1 | v_glockred | sib |
| v_hegrenade | gren | A7 | v_hegrenade | exact |
| v_knife | knife | K1 | knives cluster | sib |
| v_m249 | mg | A1+foregrip | — | infer (спец) |
| v_m3 | shotgun | A1(туг.) | v_m3 | exact |
| v_m4a1 | rifle | A1/A2 | v_m4a1 | exact~ |
| v_mac10 | smg | A3(інфер) | — | infer |
| v_mp5 | smg | A3 | v_mp5 | exact |
| v_p228 | pistol | A1/A2 | — | infer (двійник=bind) |
| v_p90 | smg | A6 | v_p90 | exact |
| v_scout | sniper | A5 | v_scout | exact |
| v_sg550 | sniper | R1 | v_sg550 | exact |
| v_sg552 | rifle | A2(туг.) | v_sg552 | exact |
| v_smokegrenade | gren | A7 | v_smokegrenade | exact |
| v_tmp | smg | A1 | v_tmp | exact |
| v_ump45 | smg | R1 | v_ump45 | exact |
| v_usp | pistol | A2 | v_usp | exact |
| v_xm1014 | shotgun | A1 | v_xm1014 | exact |

Підсумок: **21/28 надійно** (архетип уже захоплено на нашій руці), 6 infer, 1 спец.
A1 покриває ~третину арсеналу. Застереження: **клас не однозначно визначає архетип**
(пістолети діляться A1 vs A2 vs R1; SMG — A1/A3/A6) — для автопідбору потрібна геометрія
руків'я, не лише клас (див. верифікацію).

Скрипти обміру: `tmp/testing/` (grip_survey, grip_clusters, valve_map — throwaway).

---

## 8. Верифікація: геометрія руків'я НЕ визначає архетип

Перевіряли, чи можна автопідбирати архетип із форми руків'я (щоб не тегувати вручну).
Виміряли обхват (girth) і довжину грип-руків'я з меша, спозованого в idle:

- **corr(girth, тугість хвату) = +0.42** — слабка й НЕ того знаку (фізика дала б негативну);
- **contra-приклад scout**: найтонше руків'я (girth 1.19) → найслабший хват (PIP 102);
  sg552 теж тонке (1.21) → найтугіший (211). Тонке дало і кволий, і лютий хват;
- обхвати всіх стволів у вузькому 1.2–2.3, а PIP-сума 102–227 → товщина не важіль;
- архетипи **накладаються** в геометрії (A1/A3/A4/A6 ≈ girth 1.7, PIP 173/129/133/182).

**Висновок:** хват — не функція форми руків'я, а **дискретний художній вибір**, який
автори перевикористали. Автопідбір архетипу з геометрії неможливий. Єдиний робочий
спостережуваний сигнал — **одно/дворучність** (R1 vs решта).

### Верифікація перевикористання (leave-one-out)
Ховали ствол, відновлювали його повний хват (10-вектор кутів) як центроїд інших членів
архетипу:
- **ak47 (A1)**: RMS **2.7°**, запас до чужого архетипу ~3× — відмінно;
- **fiveseven (R1)**: RMS **4.4°** — добре;
- **sg552 (A2)**: RMS 8.7°, до A2=27.4 vs A1=30.6 — **прикордонний** (майже рівновіддалений).
Тобто захоплена поза перевикористовується в межах ~3–9° (у межах природного розкиду).

---

## 9. Автоматизація: вердикт і конвеєр

**Повністю автоматична «геометрія → хват» — неможлива** (див. §8). Але **асистована
«1 тег → готова модель»** — так, і потужна: незводима ручна праця = **один вибір з ~8
архетипів на ствол** (для 21/28 — автоматом з таблиці §7). Усе інше — автоматика.

### Інструмент `tmp/testing/retarget.py`
Генералізація Свопу 2 (`build_variantB.py`):
```
retarget.py <weapon> [--archetype A1] [--class rifle] [--out DIR] [--compile]
```
- ім'я резолвиться в `tmp/cso_nexon` або `tmp/valve_original`;
- автодетекція: idle, меш зброї (по кістках), аніми, текстури; генерує компільований QC
  (секвенції, fps по ключу назви);
- зброя рідна (спозована в idle[0]), руки — за режимом (нижче).
Перевірено end-to-end (studiomdl): deagle, ak47 компілюються чисто.

### Режими хвату
- **`self` (дефолт)** — рука позується з **власного idle цілі**; для native-зброї це
  правильно й безпечно для reload. Використовує ВЛАСНИЙ hand-меш цілі (див. нижче чому).
- **`--archetype <ID>`** — грипову (ліву) руку містком беремо з grip-джерела архетипу,
  support (праву) лишаємо нативною. Для foreign-зброї без свого хвату.

### Діагноз reload (важливо)
Архетип захоплює **лише грипову руку**. Support-рука своя в кожної зброї (у пістолета
права MCP ~15–30 обгортає руків'я поряд; у гвинтівки ~100 переламана на цівці). Якщо
запекти пістолетну support-руку й драйвити reload гвинтівки — з ~7 кадру ламається.
Тому support-рука мусить іти з **власного idle цілі**.

---

## 10. Approach B — дельта-ретаргет пальців (поточний фронт роботи)

**Мета:** одна **уніфікована** рука на всіх стволах (під bodygroup-merge, `pev->body` ≤
255: 2 руки × 32 зброї = 64) + рідна поведінка анімацій + без викруту.

### Чому не простіше
- **Місток (жорсткий зсув)** — без викруту, але заморожує конфіг → reload support-руки ламається.
- **Пряме перепозування чужого меша на інший скелет** — викручує: bind-пози (локальні осі
  кісток) різних пакетів відрізняються **~25–30°** (виміряно automag↔ak47). Алгебра LBS:
  `W_tgtFrame · B_canon⁻¹` неминуче тягне цю різницю. Тобто одну руку **не** можна дешево
  перепозувати в чужий конфіг без викруту.

### Механізм B (`tmp/testing/retarget_delta.py`)
Канонічна рука лишається у ВЛАСНОМУ конфізі пальців (0 викруту — FK на власному bind);
зап'ясток+зброя+корінь драйвляться **рідною** анімацією цілі; переписуємо лише **локальні
обертання пальців** — переносимо цільову **дельту руху**, спряжену під конвенцію канона:
```
L_out[b][f] = Rc0[b] · ( C_b · (Rt0[b]⁻¹ · Rt[b][f]) · C_b⁻¹ ),   C_b = Rc0[b] · Rt0[b]⁻¹
pos_out[b]  = довжина кістки канона (стала)
```
На idle (дельта=identity) пальці в канон-конфізі → уніформно, без викруту. reload/shoot
переносять рух суглобів. Зап'ясток нативний → support-рука все одно йде на магазин.

### Валідація (ak47)
- idle R idxPIP = **66.5** = точно канон automag (66.5) → **0 викруту**;
- native reload delta (f0→f10) = **−39.6°**; наш вихід = **−39.6°** (перенесено 1:1);
- **0 NaN** по 91 кадру; уся геометрія (2140 тр.) на місці (палм фолдиться на зап'ясток цілі).

### Статус: ПОКРАЩЕННЯ Є, ПОТРІБНЕ ДООПРАЦЮВАННЯ
**Відкритий дефект (пауза 2026-08-05):** текстуру **правого зап'ястка викручує**. Причина —
канон зараз `automag hand_High` (пістолет): у нього **немає правого `Bone26`**, права рука
пістолетна, і палм `Bone_Se_Hand-R` фолдиться на зап'ясток ak47 з ~30° різницею орієнтації
bind → жорсткий проворот. Ліва (грип A1) чиста; правий зап'ясток — ні. Це **дефект**, не
«м'яке відхилення» — треба усунути.

### Наступні кроки (варіанти)
1. **Канон → `storage/hands` (male/female, Bip01)** — справжня уніфікована рука; треба мапа
   **Bip01↔CSO**; прибирає дефекти automag (обидві руки повні). **Рекомендовано** (цільове).
2. Канон по класу: повна CSO-рука дворучної гвинтівки для гвинтівок тощо.
3. Патч: корекція розвороту при фолді палма (милиця).

### Артефакти (Approach B / інструменти)
- `tmp/testing/retarget.py` — генералізований конвеєр (self / archetype режими).
- `tmp/testing/retarget_delta.py` — Approach B (дельта-ретаргет пальців).
- `tmp/testing/retarget_delta_out/v_ak47/v_ak47.mdl` — поточний результат (лівий бік ок,
  правий зап'ясток викручений — до виправлення).
- Обмірні/діагностичні скрипти в `tmp/testing/` — throwaway.
