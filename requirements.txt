# valve-qc-merger — runtime requirements
#
# NONE: the toolkit is pure standard-library Python (3.11+ for tomllib).
# Install the package itself with:
#   pip install -e .
#
# The retarget pipeline additionally needs Blender (its worker runs inside
# Blender's own bundled Python and imports bpy/mathutils from there — they
# are NOT pip packages). merge-view / merge-player / merge-world need only
# this package and, to compile the output, a studiomdl binary
# (tools/build_studiomdl.sh builds one on macOS).

# --- development tools (same versions as pyproject [project.optional-dependencies]) ---
# Install with:  pip install -r requirements.txt   (or: pip install -e '.[dev]')
pytest>=8.0
ruff>=0.5
mypy>=1.10
